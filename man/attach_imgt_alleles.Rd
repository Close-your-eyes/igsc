% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/attach_imgt_allele.R
\name{attach_imgt_alleles}
\alias{attach_imgt_alleles}
\title{Attach matching IMGT alleles to TCR sequences}
\usage{
attach_imgt_alleles(
  seq,
  imgt_ref,
  pick.by = "alignment",
  lapply_fun = lapply,
  seq_col = "consensus_seq_cr",
  V_col = "V_cr",
  J_col = "J_cr",
  ...
)
}
\arguments{
\item{seq}{A data frame containing clonotype sequences and their V and J gene
names, or a character vector of nucleotide sequences. Character-vector
names, when present and non-missing, must contain a V and J name separated
by \code{"___"}.}

\item{imgt_ref}{An IMGT reference data frame containing columns \code{seq.nt} and
\code{Allele}, or a named character vector whose values are reference nucleotide
sequences and whose names are allele names. A data frame produced by
\code{\link[=imgt_tcr_segment_prep]{imgt_tcr_segment_prep()}} is suitable.}

\item{pick.by}{Method used when multiple candidate alleles remain. Use
\code{"alignment"} to choose the allele with the highest local alignment score,
or \code{"random"} to sample a candidate. Random selection is faster and useful
when the exact allele is not required.}

\item{lapply_fun}{Apply function, or its name, used to process sequences.
Common choices are \code{\link[base:lapply]{base::lapply()}}, \code{pbapply::pblapply()}, and
\code{\link[parallel:mclapply]{parallel::mclapply()}}.}

\item{seq_col}{Name of the sequence column when \code{seq} is a data frame.}

\item{V_col}{Name of the V-gene column when \code{seq} is a data frame.}

\item{J_col}{Name of the J-gene column when \code{seq} is a data frame.}

\item{...}{Additional arguments passed to \code{lapply_fun}, such as \code{mc.cores}
for \code{\link[parallel:mclapply]{parallel::mclapply()}}.}
}
\value{
A data frame with one row per unique input sequence and two columns:
\code{seq}, containing the input sequence, and \code{VJ_IMGT}, containing the selected
V- and J-allele names separated by \code{"___"}.
}
\description{
Find IMGT reference alleles for the V and J segments of each unique input
sequence. When V and J gene names are available, candidate reference alleles
are first narrowed by name similarity. A candidate is then chosen by local
sequence-alignment score or by random sampling.
}
\details{
Duplicate sequence values are removed before matching. For data-frame input,
names used to restrict the search are constructed from \code{V_col} and \code{J_col}.
For character-vector input, equivalent names may be supplied directly in the
form \code{"TRBV11-3___TRBJ2-5"}. Unnamed sequences are compared against all V
and J alleles in \code{imgt_ref}.
}
\examples{
\dontrun{
imgt_ref <- readRDS(system.file(
  "extdata", "IMGT_ref/human/hs.rds", package = "igsc"
))

ata <- attach_imgt_alleles(
  seq = cl_long,
  imgt_ref = imgt_ref,
  pick.by = "random",
  lapply_fun = lapply
)
cl_long <-
  dplyr::left_join(cl_long, ata, by = c("consensus_seq_cr" = "seq")) |>
  tidyr::separate(VJ_IMGT, into = c("V_imgt", "J_imgt"), sep = "___")

# Alternatively, pass an unnamed vector.
ata <- attach_imgt_alleles(
  seq = cl_long$consensus_seq_cr,
  imgt_ref = imgt_ref,
  pick.by = "random"
)

# Or provide V and J gene names on the vector to narrow the search.
named_seq <- stats::setNames(
  object = cl_long$consensus_seq_cr,
  nm = paste0(cl_long$V_cr, "___", cl_long$J_cr)
)
ata <- attach_imgt_alleles(
  seq = named_seq,
  imgt_ref = imgt_ref,
  pick.by = "random"
)
}
}
