% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hla_df_from_xml.R
\name{extract_cds}
\alias{extract_cds}
\title{Extract and optionally translate concatenated exon sequences}
\usage{
extract_cds(
  data,
  exons = NULL,
  cds_start = NULL,
  cds_end = NULL,
  translate = FALSE,
  no.init.codon = TRUE,
  if.fuzzy.codon = "X"
)
}
\arguments{
\item{data}{A data frame containing a character column named \code{seq} and exon
coordinate columns named \code{Exon1}, \code{Exon2}, etc. Coordinates must use the
one-based inclusive format \code{"start_end"}.}

\item{exons}{An optional positive integer vector specifying the exons to
extract. Exons are concatenated in the supplied order. If \code{NULL}, all
available exons are used in numeric order.}

\item{cds_start}{An optional positive integer giving the first nucleotide
to retain from the concatenated sequence.}

\item{cds_end}{An optional positive integer giving the last nucleotide
to retain from the concatenated sequence.}

\item{translate}{A single logical value indicating whether the extracted
sequence should be translated.}

\item{no.init.codon}{A logical value passed to \code{\link[Biostrings:translate]{Biostrings::translate()}}.
When Exon1 is missing and the reading frame is inferred, \code{TRUE} is used
because the extracted sequence is an internal CDS fragment.}

\item{if.fuzzy.codon}{Controls how \code{\link[Biostrings:translate]{Biostrings::translate()}} handles fuzzy
codons. The default, \code{"X"}, translates fuzzy codons to \code{"X"}.}
}
\value{
A character vector containing one nucleotide or amino-acid sequence
per row of \code{data}.
}
\description{
Extracts specified exons from genomic sequences, removes intervening
introns, and concatenates the exon sequences. The resulting nucleotide
sequence can optionally be restricted to a subsequence and translated.
}
\details{
A missing Exon1 is allowed. When \code{translate = TRUE} and Exon1 is missing,
all three possible reading frames are tested. Frames containing a stop codon
before the final translated position are rejected. Among acceptable frames,
a frame ending in a terminal stop codon is preferred; otherwise, the longest
translation is selected. Ties are resolved using the smallest nucleotide
offset and generate a warning.

Missing trailing exon annotations are allowed. However, a missing exon from
Exon2 through the highest available exon is treated as an internal gap and
produces an error.
}
\examples{
alleles <- data.frame(
  seq = "ATGAAAGGGCCCTAA",
  Exon1 = "1_6",
  Exon2 = "10_15"
)

extract_cds(alleles)

extract_cds(
  alleles,
  translate = TRUE,
  no.init.codon = FALSE
)

}
