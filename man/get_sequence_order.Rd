% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/order_sequences.R
\name{get_sequence_order}
\alias{get_sequence_order}
\title{Determine Sequence Order from a Distance Matrix}
\usage{
get_sequence_order(distmat, method = c("tsp", "seriate", "treeline"), ...)
}
\arguments{
\item{distmat}{A distance matrix or an object coercible to a
\code{\link[TSP:TSP]{TSP::TSP()}} object. For \code{method = "treeline"}, it must be accepted by
\code{\link[DECIPHER:Treeline]{DECIPHER::Treeline()}}.}

\item{method}{Character string specifying the ordering method. \code{"tsp"}
uses a TSP solver, while \code{"treeline"} derives the order from clusters
produced by a neighbor-joining tree.}
}
\value{
An integer vector containing the sequence indices in their
calculated order.
}
\description{
Computes an ordering of sequences using either a traveling salesperson
problem (TSP) solver or neighbor-joining tree clustering.
}
\examples{
distances <- stats::dist(USArrests[1:10, ])
get_sequence_order(distances, method = "tsp")

\dontrun{
get_sequence_order(as.matrix(distances), method = "treeline")
}
}
