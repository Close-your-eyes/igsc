% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_tcrdist3.R
\name{run_tcrdist3}
\alias{run_tcrdist3}
\title{Calculate TCRdist3 distance matrices}
\usage{
run_tcrdist3(
  input,
  script = system.file("extdata", "run_tcrdist3.py", package = "igsc"),
  db_file = "alphabeta_gammadelta_db.tsv",
  chain = c("TRB", "TRA"),
  species = c("human", "mouse"),
  suffix = "",
  workdir = getwd(),
  python = "/opt/local/bin/python3"
)
}
\arguments{
\item{input}{Character scalar giving the path to the input CSV file.
A relative path is interpreted relative to \code{workdir}.}

\item{script}{Character scalar giving the path to the Python script that
performs the TCRdist3 calculation. By default, the script bundled with
the \pkg{igsc} package is used.}

\item{db_file}{Character scalar giving the path to the TCRdist3 reference
database file. A relative path is interpreted relative to \code{workdir}.}

\item{chain}{Character scalar specifying the receptor chain to process.
Must be \code{"TRB"} or \code{"TRA"}. The default is \code{"TRB"}.}

\item{species}{Character scalar specifying the organism. Must be
\code{"human"} or \code{"mouse"}. Gene names in \code{input} must be appropriate for
the selected species.}

\item{suffix}{Character scalar containing an optional suffix to append to
the output filenames. For example, \code{suffix = "sample1"} produces a
filename ending in \verb{_sample1.csv}. The default, \code{""}, adds no suffix.}

\item{workdir}{Character scalar giving the working directory used to
resolve relative input paths and store output files. The directory must
already exist.}

\item{python}{Character scalar giving the path to a Python 3 executable.
The Python environment must contain the \code{pandas} and \code{tcrdist3}
packages.}
}
\value{
Invisibly returns a named character vector containing the paths
to the two output files. Element \code{full} is the full TCRdist matrix and
element \code{cdr3} is the CDR3-only distance matrix.
}
\description{
Runs the bundled Python script to calculate a full TCRdist distance
matrix and a CDR3-only distance matrix for either T-cell receptor alpha
(TRA) or beta (TRB) sequences.
https://github.com/kmayerb/tcrdist3
}
\details{
The input must be a comma-separated text file with a header row. Required
columns depend on \code{chain}:

\tabular{llll}{
Chain \tab CDR3 amino acid sequence \tab V gene \tab J gene \cr
\code{"TRA"} \tab \code{cdr3_a_aa} \tab \code{v_a_gene} \tab \code{j_a_gene} \cr
\code{"TRB"} \tab \code{cdr3_b_aa} \tab \code{v_b_gene} \tab \code{j_b_gene}
}

Rows with missing values in any required column are removed. CDR3 amino
acid sequences are converted to uppercase, and surrounding whitespace is
removed from the CDR3, V-gene, and J-gene columns.

The input may optionally contain a \code{count} column giving the abundance of
each receptor. If it is absent, every row is assigned a count of one.
Additional columns are allowed and are passed to TCRdist3 unchanged.

V- and J-gene names must use nomenclature recognized by TCRdist3 and must
be compatible with \code{species} and \code{db_file}.

Two CSV files are written to \code{workdir}:
\itemize{
\item \verb{tcrdist3_full_distmat_alpha[_suffix].csv} or
\verb{tcrdist3_full_distmat_beta[_suffix].csv}
\item \verb{tcrdist3_cdr3_distmat_alpha[_suffix].csv} or
\verb{tcrdist3_cdr3_distmat_beta[_suffix].csv}
}

Existing files with the same names are overwritten.
}
