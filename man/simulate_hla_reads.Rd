% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hla_typing.R
\name{simulate_hla_reads}
\alias{simulate_hla_reads}
\title{Simulate reads from two HLA-A alleles}
\usage{
simulate_hla_reads(
  hla_ref,
  gene = c("A", "B", "C", "E", "F", "G", "DRA", "DRB1", "DRB3", "DRB4", "DRB5", "DPA1",
    "DPB1", "DQA1", "DQB1", "DMA", "DMB", "DOA", "DOB"),
  n = 1000L,
  read_length = 100L,
  allele_names = NULL,
  proportions = c(0.55, 0.45),
  snps_per_read = 0L,
  minus_strand_prob = 0,
  seq_col = "seq_Exon2_3",
  allele_col = "allele",
  seed = 42L
)
}
\arguments{
\item{hla_ref}{A data frame containing HLA reference alleles and their
sequences.}

\item{gene}{which HLA gene to sample from}

\item{n}{A positive integer giving the number of reads to generate.}

\item{read_length}{A positive integer giving the read length in nucleotides.}

\item{allele_names}{A character vector of HLA allele
names, or \code{NULL} to use random alleles in \code{hla_ref}.}

\item{proportions}{A numeric vector giving the relative
sampling probabilities of \code{allele_names}. Values must be non-negative and
have a positive sum; they are normalized internally.}

\item{snps_per_read}{A non-negative vector of integers giving allowed number
of single-nucleotide substitutions inserted into reads. It cannot exceed
\code{read_length}. The default of zero produces exact reference matches.}

\item{minus_strand_prob}{probability of reads on minus strand. these will be
reverse-complemented}

\item{seq_col}{A character scalar naming the reference sequence column in
\code{hla_ref}.}

\item{allele_col}{A character scalar naming the allele column in \code{hla_ref}.}

\item{seed}{An integer seed passed to \code{\link[base:set.seed]{base::set.seed()}} for reproducible
sampling.}
}
\value{
A data frame with \code{n} rows and the following columns:
\describe{
\item{readName}{A unique synthetic read identifier.}
\item{seq}{The simulated DNA sequence, including inserted SNPs.}
\item{strand}{The strand label \code{"+"}, included for compatibility with
\code{\link[=hla_typing]{hla_typing()}}.}
\item{true_allele}{The reference allele from which the read was sampled.}
\item{reference_start}{The one-based start position in the reference
sequence.}
\item{n_snps}{The number of substitutions inserted into the read.}
\item{snp_positions}{A list-column containing the one-based positions of
inserted substitutions within each read.}
}
}
\description{
Generate fixed-length synthetic DNA reads by sampling substrings from two
HLA-A reference alleles. Reads are sampled according to the requested allele
proportions and can contain a fixed number of randomly positioned
single-nucleotide substitutions. The resulting data frame can be supplied
directly to \code{\link[=hla_typing]{hla_typing()}}.
}
\details{
Reference RNA sequences are converted from \code{U} to \code{T}. Reference rows with
missing sequences, characters other than \code{A}, \code{C}, \code{G}, and \code{T}, or
sequences shorter than \code{read_length} are excluded. When \code{allele_names} is
\code{NULL}, the first two suitable HLA-A alleles are used.

SNP positions are sampled without replacement within each read. Each
selected nucleotide is replaced by one of the other three DNA bases, so the
number of substitutions in every read is exactly \code{snps_per_read}.

Add decoy reads by running function twice and combine different allele_names.
}
\examples{
\dontrun{
reads <- simulate_hla_a_reads(
  hla_ref,
  n = 1000,
  read_length = 100,
  snps_per_read = 2
)

table(reads$true_allele)
result <- hla_typing(hla_ref, reads, maxmis = 2)
}
}
\seealso{
\code{\link[=hla_typing]{hla_typing()}}
}
