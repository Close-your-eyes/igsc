% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hla_typing_from_bam.R
\name{get_hla_reads}
\alias{get_hla_reads}
\title{Extract candidate HLA reads from an RNA-seq BAM file}
\usage{
get_hla_reads(
  bam,
  reference_genome = NULL,
  reference_gtf = NULL,
  regions = NULL,
  genes = c("A", "B", "C"),
  mhc_start = 28000000L,
  mhc_end = 34000000L,
  include_alt_contigs = TRUE,
  tags = c("CR", "CB", "CY", "AS", "UR", "UB", "UY", "HI", "NH", "nM", "RE"),
  scores = TRUE,
  min_mapq = 0L,
  primary_only = TRUE,
  cell_barcodes = NULL,
  cell_barcode_tag = "CB"
)
}
\arguments{
\item{bam}{Path to a coordinate-sorted BAM file with a corresponding index.}

\item{reference_genome}{Optional path to the FASTA file used for alignment.
The FASTA must have a \code{.fai} index and is used to validate reference names
and lengths.}

\item{reference_gtf}{Optional path to a reference GTF file. When \code{regions}
is \code{NULL}, gene records are read with \code{\link[=read_gtf]{read_gtf()}} and the annotated
boundaries of \code{genes} are used as the query ranges.}

\item{regions}{Optional \code{\link[GenomicRanges:GRanges]{GenomicRanges::GRanges()}} object. Explicit regions
take precedence over \code{reference_gtf}. When both are \code{NULL}, the chromosome
6 interval specified by \code{mhc_start} and \code{mhc_end} is used and, when
\code{include_alt_contigs = TRUE}, chromosome 6 alternative and HLA-named
contigs are added.}

\item{genes}{Character vector of HLA genes whose GTF boundaries should be
queried, for example \code{c("A", "B", "C")} or \code{c("HLA-A", "HLA-B")}.
This argument is used only when \code{reference_gtf} is supplied and \code{regions}
is \code{NULL}.}

\item{mhc_start, mhc_end}{One-based inclusive bounds of the broad MHC interval
used when \code{regions} is \code{NULL}.}

\item{include_alt_contigs}{Logical; include whole BAM reference sequences
whose names identify them as chromosome 6 alternative or HLA contigs.}

\item{tags}{BAM tags passed to \code{\link[=get_bam_reads]{get_bam_reads()}}. The default includes common
Cell Ranger cell-barcode, UMI, and alignment tags.}

\item{scores}{Logical; calculate base-quality summaries in
\code{\link[=get_bam_reads]{get_bam_reads()}}.}

\item{min_mapq}{Minimum mapping quality to retain.}

\item{primary_only}{Logical; remove secondary and supplementary alignments.}

\item{cell_barcodes}{Optional character vector of corrected cell barcodes to
retain. This requires the tag named by \code{cell_barcode_tag}.}

\item{cell_barcode_tag}{Name of the BAM tag containing corrected cell
barcodes.}
}
\value{
A data frame in the format returned by \code{\link[=get_bam_reads]{get_bam_reads()}}, with a
unique \code{readName} column for direct use by \code{\link[=hla_typing]{hla_typing()}}. The queried
ranges are stored in the \code{hla_regions} attribute.
}
\description{
Extract alignments from the chromosome 6 major histocompatibility complex
(MHC), together with chromosome 6 alternative or HLA-named contigs. This
provides a compact set of candidate HLA reads suitable for \code{\link[=hla_typing]{hla_typing()}}.
}
\details{
The default interval, 28--34 Mb on chromosome 6, deliberately extends beyond
the classical HLA genes. It works for both GRCh37 and GRCh38 and avoids
relying on exact exon coordinates, which can lose informative reads near
splice junctions or reads placed at a related HLA locus. Alternatively,
\code{reference_gtf} can be supplied to query only the annotated boundaries of
the genes in \code{genes}. Explicit \code{regions} take precedence over both methods.

When \code{reference_genome} is supplied it must be an indexed FASTA file. Its
sequence names and lengths are checked against the BAM header. A FASTA is
not otherwise required because a BAM already records the reference sequence
names and lengths used for alignment.

Only alignments represented in the BAM can be recovered. Unmapped reads and
reads aligned outside the selected regions are not classified as HLA reads by
this function.
}
\examples{
\dontrun{
reads_hla <- get_hla_reads(
  bam = "outs/possorted_genome_bam.bam",
  reference_genome = "reference/fasta/genome.fa",
  reference_gtf = "reference/genes/genes.gtf",
  genes = "HLA-A"
)

type_a <- hla_typing(
  hla_ref = subset(hla_ref, gene == "A"),
  reads = reads_hla,
  read_name_col_name = "readName"
)
}
}
