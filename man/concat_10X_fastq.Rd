% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/concat_10X_fastq.R
\name{concat_10X_fastq}
\alias{concat_10X_fastq}
\title{Concatenate lane-split 10x FASTQ files}
\usage{
concat_10X_fastq(
  dir,
  save_path,
  exec = FALSE,
  existing = c("error", "skip", "overwrite"),
  create_save_path = TRUE,
  verify = TRUE
)
}
\arguments{
\item{dir}{Directory searched recursively for input FASTQ files.}

\item{save_path}{Parent directory for sample-specific output directories.}

\item{exec}{Logical; execute commands when \code{TRUE}, otherwise return a plan.}

\item{existing}{How existing output files are handled: \code{"error"}, \code{"skip"},
or \code{"overwrite"}.}

\item{create_save_path}{Logical; create \code{save_path} when it does not exist.}

\item{verify}{Logical; after execution, verify that output size equals the
sum of input file sizes.}
}
\value{
A data frame containing output files, input files, commands, and
execution status.
}
\description{
Concatenates gzip-compressed FASTQ files belonging to the same sample and
read type. Concatenating gzip streams directly is valid: the resulting file
is a multi-member gzip archive.
}
\details{
Expected input filenames follow this structure:
\code{sample_S1_L001_R1_001.fastq.gz}
}
\examples{
\dontrun{
plan <- concat_10X_fastq(
  dir = "/path/to/fastq",
  save_path = "/path/to/output"
)

result <- concat_10X_fastq(
  dir = "/path/to/fastq",
  save_path = "/path/to/output",
  exec = TRUE,
  existing = "error"
)
}
}
