% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hla_df_from_xml.R
\name{hla_df_from_xml}
\alias{hla_df_from_xml}
\title{Prepare an HLA allele data frame from an XML file}
\usage{
hla_df_from_xml(file_path, lapply_fun = lapply, replace_none_pg = TRUE, ...)
}
\arguments{
\item{file_path}{Path to an \code{.xml}, \code{.xml.gz}, or \code{.xml.zip} file.}

\item{lapply_fun}{Function used to process allele nodes. Suitable choices
include \code{\link[base:lapply]{base::lapply()}}, \code{pbapply::pblapply}, and
\code{parallel::mclapply}.}

\item{replace_none_pg}{Logical. Replace \code{"None"} in \code{p_group} and \code{g_group}
with \code{allele_protein} and \code{allele_coding}, respectively.}

\item{...}{Additional arguments passed to \code{lapply_fun}, such as \code{mc.cores}.}
}
\value{
A data frame with one row per allele.
}
\description{
Reads allele information from an IPD-IMGT/HLA XML file. The input may be
uncompressed (\code{.xml}), gzip-compressed (\code{.xml.gz}), or ZIP-compressed
(\code{.xml.zip}).
}
\details{
ZIP archives must contain a file named \code{hla.xml}, or exactly one XML file.
}
\examples{
\dontrun{
hla <- hla_df_from_xml("hla.xml")
hla <- hla_df_from_xml("hla.xml.gz")
hla <- hla_df_from_xml("hla.xml.zip")

hla <- hla_df_from_xml(
  "hla.xml.zip",
  lapply_fun = parallel::mclapply,
  mc.cores = parallel::detectCores()
)
}
}
