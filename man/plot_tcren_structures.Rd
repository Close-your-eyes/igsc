% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tcren_structure_plots.R
\name{plot_tcren_structures}
\alias{plot_tcren_structures}
\title{Summarize and visualize TCRen structure scores}
\usage{
plot_tcren_structures(
  features,
  scores,
  top_n = 10L,
  rank_by = c("P_native", "Q", "T"),
  label_top = TRUE,
  point_alpha = 0.85
)
}
\arguments{
\item{features}{Either a data frame containing TCRen features or a path to a
delimited feature file readable by \code{\link[vroom:vroom]{vroom::vroom()}}. The table must contain
one row per \code{complex.id}.}

\item{scores}{Either a data frame containing TCRen scores or a path to a
delimited score file readable by \code{\link[vroom:vroom]{vroom::vroom()}}. The table must contain
one row per \code{complex.id} and columns \code{Q} and \code{T}. \code{P_native} is optional.}

\item{top_n}{Positive integer giving the number of highest-ranked structures
to label and return in \code{top_candidates}. Defaults to \code{10}.}

\item{rank_by}{Character vector specifying the preferred ranking-score order.
The first column present in the joined data with at least one finite value
is used. By default, \code{P_native} is preferred, followed by \code{Q} and \code{T}. This
fallback is useful when TCRen skips \code{P_native} for a small cohort.}

\item{label_top}{Logical; if \code{TRUE}, label the top candidates on every plot.}

\item{point_alpha}{Numeric point opacity between zero and one.}
}
\value{
A named list with four elements:
\describe{
\item{\code{data}}{A named list containing \code{features}, \code{scores}, \code{combined},
\code{ranking}, and \code{top_candidates} data frames.}
\item{\code{plots}}{A named list containing the ggplot objects
\code{geometry_vs_topology}, \code{geometry_vs_contact_energetics},
\code{burial_vs_hydrogen_bonds}, and
\code{footprint_contiguity_vs_diversity}.}
\item{\code{ranking_score}}{The score column selected from \code{rank_by}.}
\item{\code{top_n}}{The effective number of top candidates returned.}
}
}
\description{
Join a TCRen feature table to a TCRen score table, rank candidate
TCR-pMHC structures, and construct four diagnostic plots covering docking
geometry, contact energetics, interface quality, and footprint topology.
}
\details{
\code{F_good} is defined as \code{-(F_tcr_pep + F_tcr_mhc)}, so larger values represent
more favorable TCRen contact scores. \code{footprint_contiguity} is
\code{-fp_b0_frac_r7}, so larger values represent a less fragmented footprint.

\code{P_native} is cohort-relative and should only be compared among structures
generated for the same biological question and with a consistent modeling
protocol. The function does not treat a missing \code{P_native} column as an
error; it falls back according to \code{rank_by}.
}
\examples{
\dontrun{
result <- plot_tcren_structures(
  features = "features.tsv",
  scores = "scores.tsv",
  top_n = 10
)

result$data$ranking
result$plots$geometry_vs_topology
result$plots$geometry_vs_contact_energetics
}
}
