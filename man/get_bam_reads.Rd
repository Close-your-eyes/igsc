% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_bam_reads.R
\name{get_bam_reads}
\alias{get_bam_reads}
\title{Read BAM alignments overlapping genomic ranges}
\usage{
get_bam_reads(
  bam,
  granges = GenomicRanges::GRanges(seqnames =
    as.character(Rsamtools::idxstatsBam(bam)[which.max(Rsamtools::idxstatsBam(bam)[["mapped"]]),
    "seqnames"]), ranges = "1..10000"),
  tags = c("CR", "CB", "CY", "AS", "UR", "UB", "UY", "HI", "NH", "nM", "RE"),
  scores = T,
  revcomp_minus_strand = F,
  revcomp_plus_strand = F
)
}
\arguments{
\item{bam}{Path to a coordinate-sorted BAM file with an index accessible to
Rsamtools.}

\item{granges}{A \code{\link[GenomicRanges:GRanges]{GenomicRanges::GRanges()}} object specifying the ranges to
query. Its strand is ignored when selecting alignments. The default queries
bases 1 through 10,000 of the reference sequence with the highest mapped
read count in the BAM index.}

\item{tags}{Character vector of BAM tag names passed to
\code{\link[Rsamtools:ScanBamParam]{Rsamtools::ScanBamParam()}}. Use \code{character(0)} to request no tags. The
default requests common Cell Ranger barcode, UMI, and alignment tags;
unavailable tags are omitted from the result and reported in a message.}

\item{scores}{Logical; if \code{TRUE}, calculate per-read Phred-quality summaries
from \code{qual}.}

\item{revcomp_minus_strand}{Logical; passed as \code{reverseComplement} to
\code{\link[Rsamtools:ScanBamParam]{Rsamtools::ScanBamParam()}}. If \code{TRUE}, reverse-complement \code{seq} and reverse
\code{qual} for alignments on the minus strand.}

\item{revcomp_plus_strand}{Logical; if \code{TRUE}, reverse-complement \code{seq} and
reverse \code{qual} for alignments on the plus strand after reading the BAM.}
}
\value{
A data frame containing fields returned by
\code{\link[Rsamtools:scanBamWhat]{Rsamtools::scanBamWhat()}}, including \code{qname}, \code{flag}, \code{rname}, \code{strand},
\code{pos}, \code{mapq}, \code{cigar}, \code{seq}, and \code{qual}. \code{genomic_range} is the character
form of the one-based queried-range index. Requested tags present in the BAM are
added as columns. When \code{scores = TRUE}, four more columns are added:
\code{readQualNum} (a list of per-base Phred scores), \code{minQual}, \code{meanQual}, and
\code{n_belowQ30} (the number of bases with Phred score below 30).
}
\description{
Extract alignments with \code{\link[Rsamtools:scanBam]{Rsamtools::scanBam()}} and return one data-frame row
per alignment found in each requested range. The function does not filter by
mapping quality, alignment flag, or cell barcode.
}
\details{
A coordinate-sorted, indexed BAM is needed for range queries. The sequence
names in \code{granges} must match those in the BAM header (for example, \code{chr6}
and \code{6} are different names). If \code{granges} is omitted, only the first 10,000
bases of the BAM reference sequence with the most mapped reads are queried;
this is not a whole-BAM extraction. Range coordinates are one-based and
inclusive. The strand stored in \code{granges} does not restrict alignments.

A read overlapping two input ranges can appear twice. Paired ends and
secondary or supplementary alignments can also share a \code{qname}. This function
does not deduplicate or create unique read names. For HLA-specific extraction
with additional filtering, see \code{\link[=get_hla_reads]{get_hla_reads()}}. Tight gene or exon ranges
may miss reads aligned nearby or to a related locus.

The default tags include \code{CR}/\code{CB} (raw/corrected cell barcode), \code{UR}/\code{UB}
(raw/corrected UMI), \code{AS} (alignment score), and \code{NH} (number of reported
alignments), among others. Tag columns are present only when the BAM provides
them.

BAM stores sequences aligned to the minus strand in reference orientation.
With both reverse-complement options \code{FALSE}, \code{seq} and \code{qual} remain in that
BAM orientation. \code{revcomp_minus_strand = TRUE} asks Rsamtools to return
minus-strand reads in sequenced orientation. \code{revcomp_plus_strand = TRUE}
additionally reverse-complements plus-strand read sequences and reverses
their quality strings. Setting both to \code{TRUE} puts returned sequences in the
orientation opposite to the reference plus strand, which can be useful when
comparing with a minus-strand reference sequence.
}
\examples{
\dontrun{
bam <- "path/to/possorted_genome_bam.bam"
reference_names <- names(Rsamtools::scanBamHeader(bam)[[1]]$targets)
chr6 <- intersect(c("chr6", "6"), reference_names)[1]
stopifnot(!is.na(chr6))

mhc <- GenomicRanges::GRanges(
  seqnames = chr6,
  ranges = IRanges::IRanges(start = 29000000L, end = 35000000L)
)
reads <- get_bam_reads(bam, granges = mhc, tags = c("CB", "UB"))
reads <- reads[!is.na(reads$mapq) & reads$mapq >= 20, , drop = FALSE]
head(reads[, c("qname", "rname", "pos", "mapq", "seq")])
}
}
\seealso{
\code{\link[=get_hla_reads]{get_hla_reads()}}
}
