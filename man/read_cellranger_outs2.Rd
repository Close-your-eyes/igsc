% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_cellranger_outs2.R
\name{read_cellranger_outs2}
\alias{read_cellranger_outs2}
\title{Read and combine V(D)J contig and consensus annotations from Cell Ranger output}
\usage{
read_cellranger_outs2(vdj_outs_path, skip_incomplete = F, include_fasta = T)
}
\arguments{
\item{vdj_outs_path}{A named character vector of paths to Cell Ranger V(D)J
output directories (or directories containing the required files). Vector
names are used as sample identifiers and added as the \code{sample} column in
the returned data frame.}

\item{skip_incomplete}{Logical; if \code{TRUE}, samples missing one or more
required files are skipped. If \code{FALSE} (default), the function stops with
an error when required files are missing.}

\item{include_fasta}{Logical; if \code{TRUE} (default), contig and consensus
nucleotide sequences are read from the corresponding FASTA files and added
as \code{contig_seq} and \code{consensus_seq}. If \code{FALSE}, only annotation files are
read.}
}
\value{
A data frame containing one row per filtered contig. Depending on
\code{include_fasta}, the returned data frame includes:
\itemize{
\item contig annotations from
\code{filtered_contig_annotations.csv},
\item consensus annotations from
\code{consensus_annotations.csv},
\item contig nucleotide sequences (\code{contig_seq}),
\item consensus nucleotide sequences (\code{consensus_seq}), and
\item consensus CDR3 amino acid (\code{cdr3_consensus}) and nucleotide
(\code{cdr3_nt_consensus}) sequences.
}
}
\description{
Reads V(D)J output files generated by 10x Genomics Cell Ranger and returns a
single data frame containing contig-level annotations together with
clonotype consensus annotations. Optionally, nucleotide sequences from the
contig and consensus FASTA files can also be included.
}
\details{
Compared with \code{read_cellranger_outs()}, this function preserves the original
contig-level CDR3 assignments and does not propagate consensus CDR3 sequences
to all contigs within the same clonotype. Consensus CDR3 information is
returned in the separate columns \code{cdr3_consensus} and
\code{cdr3_nt_consensus}.

The following files are required for each sample:
\itemize{
\item Always required:
\itemize{
\item \code{filtered_contig_annotations.csv}
\item \code{consensus_annotations.csv}
}
\item Additionally required when \code{include_fasta = TRUE}:
\itemize{
\item \code{filtered_contig.fasta}
\item \code{consensus.fasta}
}
}
}
\seealso{
read_cellranger_outs()
}
