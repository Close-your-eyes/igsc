% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_clonotype_names.R
\name{add_clonotype_names}
\alias{add_clonotype_names}
\title{Assign Memorable Names to T-Cell Receptor Clonotypes}
\usage{
add_clonotype_names(
  cl_long,
  single_chains_only = T,
  strict_TCR_biology = F,
  same_cl_name_across_donor_only_when_full_annotation = T,
  names_to_avoid = NULL,
  pick_randomNames_args = list(max_iter = 10000, randomNames_args = list(which.names =
    "first")),
  sample_fields = c("virus", "donor", "genotype", "treatment"),
  sample_field_sep = "_",
  sample_col = "sample",
  barcode_col = "barcode",
  chain_col = "chain",
  cdr3_col = "cdr3",
  clonotype_col = "clonotype_id"
)
}
\arguments{
\item{cl_long}{A data frame in long format with one row per TCR-chain
annotation. It must contain the columns identified by \code{sample_col},
\code{barcode_col}, \code{chain_col}, \code{cdr3_col}, and \code{clonotype_col}. The chain
column must contain only \code{"TRA"} and \code{"TRB"}. If
\code{single_chains_only = TRUE}, columns \code{umis} and \code{reads} are also required.}

\item{single_chains_only}{A single logical value. If \code{TRUE}, retain at most
one annotation per barcode and chain, prioritizing UMI count and then read
count.}

\item{strict_TCR_biology}{A single logical value. If \code{TRUE}, do not assign
clonotype names to cells with more than one TRB chain or more than two TRA
chains, and split named clonotypes that still contain multiple TRB CDR3
sequences.}

\item{same_cl_name_across_donor_only_when_full_annotation}{A single logical
value. If \code{TRUE}, a clonotype name may be shared across donors only when
both its TRA and TRB CDR3 sequences are annotated.}

\item{names_to_avoid}{A character vector of names that must not be assigned,
or \code{NULL} to impose no additional exclusions.}

\item{pick_randomNames_args}{A named list of additional arguments passed to
\code{\link[=pick_randomNames]{pick_randomNames()}} through \code{\link[Gmisc:fastDoCall]{Gmisc::fastDoCall()}}. The function
supplies \code{n} and \code{names_to_avoid}; values supplied here should not conflict
with those arguments.}

\item{sample_fields}{A character vector naming the fields encoded in the
sample identifier, in order. It must include \code{"donor"}, and its length
must match the number of fields in every sample identifier.}

\item{sample_field_sep}{A single character string separating fields in the
sample identifier.}

\item{sample_col}{A single character string naming the sample column.}

\item{barcode_col}{A single character string naming the cell-barcode column.}

\item{chain_col}{A single character string naming the TCR-chain column.}

\item{cdr3_col}{A single character string naming the amino-acid CDR3 column.}

\item{clonotype_col}{A single character string naming the source clonotype-ID
column.}
}
\value{
A named list with three elements:
\describe{
\item{\code{cl_long}}{The input annotations with the assigned \code{cl_name}
column joined in.}
\item{\code{cl_wide}}{One row per sample and barcode, with chain-specific
annotation columns suffixed by \verb{_TRA} or \verb{_TRB}, the parsed
\code{sample_fields}, and \code{cl_name}.}
\item{\code{multi_anno_TCR_equal_umi_read}}{Rows involved in unresolved
multi-annotation ties after filtering by maximum UMI and read counts, or
\code{NULL} when no such ties are present or \code{single_chains_only = FALSE}.}
}
}
\description{
Converts a long-format T-cell receptor (TCR) annotation table to paired
alpha/beta-chain representations, assigns a random human-readable name to
each clonotype, and repeatedly merges compatible clonotypes within samples
and donors. The result contains both long- and wide-format tables.

When \code{single_chains_only = TRUE}, the function keeps the annotation with the
highest UMI count and then the highest read count for each barcode and chain.
Remaining ties are resolved deterministically by CDR3 sequence and returned
separately for inspection. When \code{strict_TCR_biology = TRUE}, cells with more
than one beta chain or more than two alpha chains are excluded from naming
and later restored with \code{NA} as their clonotype name.
}
\details{
This function relies on package-internal helpers \code{collapse_order_fun},
\code{collapse_fun2}, \code{collapse.clonotypes}, and
\code{get.cdr3.levels.per.clonotype}. Random names are generated with
\code{\link[=pick_randomNames]{pick_randomNames()}}. Messages report progress while annotations are
filtered, named, and collapsed.
}
\examples{
\dontrun{
named_clonotypes <- add_clonotype_names(tcr_annotations)

named_clonotypes$cl_long
named_clonotypes$cl_wide
}
}
